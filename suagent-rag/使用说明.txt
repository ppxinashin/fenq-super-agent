===============================================
  SuAgent-RAG 后台运行脚本 - 使用说明
===============================================

📦 已创建的文件：
---------------------------------------------
  环境管理脚本：
    - setup_env.sh      环境初始化（创建conda环境）
    - check_env.sh      环境检查
  
  服务管理脚本：
    - start.sh          启动服务（后台运行）
    - stop.sh           停止服务
    - restart.sh        重启服务
    - status.sh         查看状态
  
  日志管理脚本：
    - rotate_logs.sh    日志轮转
  
  文档：
    - DEPLOYMENT.md     详细部署文档
    - README_SCRIPTS.md 快速参考手册
    - 使用说明.txt      本文件

===============================================
  🚀 首次使用（完整流程）
===============================================

1. 初始化环境
   $ ./setup_env.sh
   
   说明：
   - 创建名为 suagent-rag 的 conda 环境
   - 使用 Python 3.13
   - 自动安装 requirements.txt 中的所有依赖
   - 仅需运行一次

2. 检查环境
   $ ./check_env.sh
   
   说明：
   - 验证 conda 环境是否正确创建
   - 检查 Python 版本
   - 检查关键依赖包是否安装

3. 启动服务
   $ ./start.sh
   
   说明：
   - 自动激活 conda 环境
   - 后台运行 main.py
   - 日志输出到 logs/suagent-rag.log

4. 查看状态
   $ ./status.sh
   
   显示：
   - 运行状态（运行中/已停止）
   - 进程 PID
   - 运行时长
   - 内存使用
   - 最近10行日志

5. 实时查看日志
   $ tail -f logs/suagent-rag.log

===============================================
  📝 日常使用
===============================================

启动服务：
  $ ./start.sh

停止服务：
  $ ./stop.sh

重启服务：
  $ ./restart.sh

查看状态：
  $ ./status.sh

查看日志：
  $ tail -f logs/suagent-rag.log        # 实时跟踪
  $ tail -n 100 logs/suagent-rag.log    # 最近100行
  $ grep ERROR logs/suagent-rag.log     # 搜索错误

轮转日志：
  $ ./rotate_logs.sh

===============================================
  🔧 高级操作
===============================================

手动激活环境：
  $ conda activate suagent-rag

更新依赖：
  $ conda activate suagent-rag
  $ pip install -r requirements.txt

重建环境：
  $ ./setup_env.sh
  （脚本会询问是否删除现有环境）

查看所有conda环境：
  $ conda env list

删除环境：
  $ conda deactivate
  $ conda env remove -n suagent-rag

===============================================
  📊 日志说明
===============================================

日志位置：
  - 主日志：logs/suagent-rag.log
  - PID文件：logs/suagent-rag.pid
  - 备份：logs/suagent-rag.log.*.gz

日志特点：
  - 所有输出（stdout和stderr）集中在一个文件
  - 自动添加时间戳
  - 自动轮转（超过100MB）
  - 保留最近10个备份

===============================================
  🐛 故障排查
===============================================

问题1：conda 命令未找到
  解决：先安装 Miniconda 或 Anaconda
  参考：https://docs.conda.io/en/latest/miniconda.html

问题2：环境不存在
  检查：$ conda env list
  解决：$ ./setup_env.sh

问题3：服务启动失败
  步骤：
    1. $ ./check_env.sh        # 检查环境
    2. $ cat logs/suagent-rag.log  # 查看错误日志
    3. $ conda activate suagent-rag
    4. $ python main.py        # 手动测试

问题4：依赖包缺失
  解决：
    $ conda activate suagent-rag
    $ pip install -r requirements.txt

问题5：PID文件错误
  解决：
    $ ./status.sh              # 先检查状态
    $ rm -f logs/suagent-rag.pid  # 手动清理
    $ ./start.sh               # 重新启动

===============================================
  🌟 最佳实践
===============================================

1. 首次部署按照"首次使用"流程操作
2. 定期运行 ./status.sh 检查服务状态
3. 定期运行 ./rotate_logs.sh 管理日志大小
4. 修改代码后使用 ./restart.sh 重启服务
5. 更新依赖后重启服务使其生效

===============================================
  ⚙️ 自动启动（可选）
===============================================

方法1: 使用 crontab
  $ crontab -e
  添加：@reboot cd /home/ubuntu/fenq-super-agent/suagent-rag && ./start.sh

方法2: 使用 systemd
  详见 DEPLOYMENT.md 中的 systemd 服务配置

===============================================
  📞 获取帮助
===============================================

快速参考：    README_SCRIPTS.md
详细文档：    DEPLOYMENT.md
环境检查：    ./check_env.sh
服务状态：    ./status.sh
查看日志：    tail -f logs/suagent-rag.log

===============================================

💡 提示：
  - 所有脚本都会自动处理 conda 环境激活
  - 无需手动激活环境后再运行脚本
  - 脚本会检查环境是否存在并给出提示

===============================================

